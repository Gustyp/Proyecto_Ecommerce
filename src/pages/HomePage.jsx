import { Container } from "react-bootstrap";

const HomePage = () => {
	return (
		<Container className="vh-100 d-flex flex-column justify-content-center align-items-center">
			<h1>Esta es mi homepage</h1>
		</Container>
	);
};

export default HomePage;
